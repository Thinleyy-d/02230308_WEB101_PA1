/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  experimental: {
    // Remove any experimental options that are causing warnings
  },
}

module.exports = nextConfig

